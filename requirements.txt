# ChEMBL ingestion — bulk (SQLite dump)
chembl_downloader==0.5.2

# Cheminformatics — fingerprints + Tanimoto
rdkit

# AWS S3 access
boto3

# Data processing / parquet
pandas
pyarrow

# Postgres
psycopg2-binary

# Airflow providers (not included in the base image by default)
apache-airflow-providers-amazon
apache-airflow-providers-postgres